Step 55 - App.jsx:
"import { useState } from 'react';\nimport { Home, Users, Map, Search, BookOpen, Heart } from 'lucide-react';\nimport { characters, locations } from './data';\nimport DetailModal from './components/DetailModal';\n\nfunction App() {\n  const [activeTab, setActiveTab] = useState('home');\n  const [searchQuery, setSearchQuery] = useState('');\n  const [selectedItem, setSelectedItem] = useState(null);\n\n  // Combine characters and locations for search and display\n  const allItems = [...characters, ...locations];\n  \n  const filteredItems = allItems.filter(item => {\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\n    if (activeTab === 'locations' && item.type !== 'local') return false;\n    if (searchQuery) {\n      return item.name.toLowerCase().includes(searchQuery.toLowerCase()) || \n             item.role?.toLowerCase().includes(searchQuery.toLowerCase());\n    }\n    return true;\n  });\n\n  const featuredItem = filteredItems.length > 0 ? filteredItems[0] : allItems[0];\n  const similarItems = filteredItems.filter(item => item.id !== featuredItem.id);\n\n  return (\n    <div className=\"app-container\">\n      {/* Sidebar */}\n      <aside className=\"sidebar\">\n        <div className=\"sidebar-logo\">\n          <BookOpen size={28} />\n        </div>\n        <div className=\"nav-links\">\n          <div className={`nav-item ${activeTab === 'home' ? 'active' : ''}`} onClick={() => setActiveTab('home')} title=\"Início\">\n            <Home size={24} />\n          </div>\n          <div className={`nav-item ${activeTab === 'characters' ? 'active' : ''}`} onClick={() => setActiveTab('characters')} title=\"Personagens\">\n            <Users size={24} />\n          </div>\n          <div className={`nav-item ${activeTab === 'locations' ? 'active' : ''}`} onClick={() => setActiveTab('locations')} title=\"Locais\">\n            <Map size={24} />\n          </div>\n        </div>\n      </aside>\n\n      {/* Main Content */}\n      <main className=\"main-content\">\n        <div cl
<truncated 2458 bytes>

========================

Step 105 - App.jsx:
"import { useState, useEffect } from 'react';\nimport { Home, Users, Map, Search, BookOpen, Settings } from 'lucide-react';\nimport DetailModal from './components/DetailModal';\nimport AdminPanel from './components/AdminPanel';\n\nfunction App() {\n  const [activeTab, setActiveTab] = useState('home');\n  const [searchQuery, setSearchQuery] = useState('');\n  const [selectedItem, setSelectedItem] = useState(null);\n  const [db, setDb] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  const fetchData = async () => {\n    try {\n      const res = await fetch('http://localhost:3001/api/data');\n      const data = await res.json();\n      setDb(data);\n      setLoading(false);\n    } catch (err) {\n      console.error('Erro ao buscar dados. O servidor node está rodando?', err);\n      setLoading(false);\n    }\n  };\n\n  useEffect(() => {\n    fetchData();\n  }, []);\n\n  const handleUpdateData = async (newData) => {\n    setDb(newData); // Update UI instantly\n    try {\n      await fetch('http://localhost:3001/api/data', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify(newData)\n      });\n    } catch (err) {\n      console.error('Erro ao salvar no servidor.', err);\n    }\n  };\n\n  if (loading || !db) return <div style={{color:'white', padding:'3rem', textAlign:'center'}}>Carregando Universo...</div>;\n\n  const allItems = [...db.characters, ...db.locations];\n  \n  const filteredItems = allItems.filter(item => {\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\n    if (activeTab === 'locations' && item.type !== 'local') return false;\n    if (searchQuery) {\n      return item.name.toLowerCase().includes(searchQuery.toLowerCase()) || \n             item.role?.toLowerCase().includes(searchQuery.toLowerCase());\n    }\n    return true;\n  });\n\n  // O item de destaque na aba \"Home\" (se não tiver busca) é sempre o LIVRO\n  const showBookCover = activeTab === 'home' && !searchQuery;\n  const fe
<truncated 4694 bytes>

========================

Step 142 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":2,\"ReplacementContent\":\"import { Home, Users, Map, Search, BookOpen, Settings, Key } from 'lucide-react';\",\"StartLine\":2,\"TargetContent\":\"import { Home, Users, Map, Search, BookOpen, Settings } from 'lucide-react';\"},{\"AllowMultiple\":false,\"EndLine\":54,\"ReplacementContent\":\"  const allItems = [...db.characters, ...db.locations, ...(db.clues || [])];\\n  \\n  const filteredItems = allItems.filter(item => {\\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\\n    if (activeTab === 'locations' && item.type !== 'local') return false;\\n    if (activeTab === 'clues' && item.type !== 'pista') return false;\\n    if (searchQuery) {\\n      return item.name.toLowerCase().includes(searchQuery.toLowerCase()) || \\n             item.role?.toLowerCase().includes(searchQuery.toLowerCase());\\n    }\\n    return true;\\n  });\",\"StartLine\":44,\"TargetContent\":\"  const allItems = [...db.characters, ...db.locations];\\n  \\n  const filteredItems = allItems.filter(item => {\\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\\n    if (activeTab === 'locations' && item.type !== 'local') return false;\\n    if (searchQuery) {\\n      return item.name.toLowerCase().includes(searchQuery.toLowerCase()) || \\n             item.role?.toLowerCase().includes(searchQuery.toLowerCase());\\n    }\\n    return true;\\n  });\"},{\"AllowMultiple\":false,\"EndLine\":77,\"ReplacementContent\":\"          <div className={`nav-item ${activeTab === 'locations' ? 'active' : ''}`} onClick={() => setActiveTab('locations')} title=\\\"Locais\\\">\\n            <Map size={24} />\\n          </div>\\n          <div className={`nav-item ${activeTab === 'clues' ? 'active' : ''}`} onClick={() => setActiveTab('clues')} title=\\\"Pistas\\\">\\n            <Key size={24} />\\n          </div>\",\"StartLine\":75,\"TargetContent\":\"          <div className={`nav-item ${activeTab === 'locations' ? 'active' : ''}`} onClick={() => setActiveTab('locations')} title=\\\"Locais\\\">\\n            <Map size={24} />\\n          </div\n<truncated 4 bytes>"

========================

Step 158 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":2,\"ReplacementContent\":\"import { Home, Users, Map, Search, BookOpen, Settings, Key, Building } from 'lucide-react';\",\"StartLine\":2,\"TargetContent\":\"import { Home, Users, Map, Search, BookOpen, Settings, Key } from 'lucide-react';\"},{\"AllowMultiple\":false,\"EndLine\":48,\"ReplacementContent\":\"  const allItems = [...db.characters, ...db.locations, ...(db.clues || []), ...(db.organizations || [])];\\n  \\n  const filteredItems = allItems.filter(item => {\\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\\n    if (activeTab === 'locations' && item.type !== 'local') return false;\\n    if (activeTab === 'clues' && item.type !== 'pista') return false;\\n    if (activeTab === 'organizations' && item.type !== 'organizacao') return false;\\n    if (searchQuery) {\",\"StartLine\":44,\"TargetContent\":\"  const allItems = [...db.characters, ...db.locations, ...(db.clues || [])];\\n  \\n  const filteredItems = allItems.filter(item => {\\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\\n    if (activeTab === 'locations' && item.type !== 'local') return false;\\n    if (activeTab === 'clues' && item.type !== 'pista') return false;\\n    if (searchQuery) {\"},{\"AllowMultiple\":false,\"EndLine\":77,\"ReplacementContent\":\"          <div className={`nav-item ${activeTab === 'locations' ? 'active' : ''}`} onClick={() => setActiveTab('locations')} title=\\\"Locais\\\">\\n            <Map size={24} />\\n          </div>\\n          <div className={`nav-item ${activeTab === 'organizations' ? 'active' : ''}`} onClick={() => setActiveTab('organizations')} title=\\\"Organizações\\\">\\n            <Building size={24} />\\n          </div>\",\"StartLine\":75,\"TargetContent\":\"          <div className={`nav-item ${activeTab === 'locations' ? 'active' : ''}`} onClick={() => setActiveTab('locations')} title=\\\"Locais\\\">\\n            <Map size={24} />\\n          </div>\"}]"

========================

Step 171 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":61,\"ReplacementContent\":\"  let featuredItem = null;\\n  let similarItems = filteredItems;\\n\\n  if (searchQuery) {\\n    featuredItem = filteredItems.length > 0 ? filteredItems[0] : null;\\n    similarItems = filteredItems.filter(item => item.id !== featuredItem?.id);\\n  } else {\\n    switch (activeTab) {\\n      case 'home':\\n        featuredItem = db.book;\\n        break;\\n      case 'characters':\\n        featuredItem = {\\n          title: \\\"Personagens\\\",\\n          author: \\\"Habitantes de Jardim das Flores\\\",\\n          category: \\\"Conheça os protagonistas e antagonistas\\\",\\n          description: \\\"Explore os perfis, motivações e segredos de cada habitante desta cidade dividida. Dos jovens revolucionários aos antigos líderes da Tríade.\\\",\\n          image: \\\"/characters_cover.png\\\",\\n          isSection: true\\n        };\\n        break;\\n      case 'locations':\\n        featuredItem = {\\n          title: \\\"Locais e Territórios\\\",\\n          author: \\\"Geografia da Guerra\\\",\\n          category: \\\"Onde tudo acontece\\\",\\n          description: \\\"Navegue pelas ruas de Jardim das Flores. Descubra as zonas neutras, os territórios sob domínio das gangues e os esconderijos secretos.\\\",\\n          image: \\\"/locations_cover.png\\\",\\n          isSection: true\\n        };\\n        break;\\n      case 'organizations':\\n        featuredItem = {\\n          title: \\\"Organizações\\\",\\n          author: \\\"Estruturas de Poder\\\",\\n          category: \\\"Facções, Comércios e Instituições\\\",\\n          description: \\\"Entenda a engrenagem que move a cidade. As três grandes gangues da Tríade, os serviços públicos corrompidos e os negócios clandestinos.\\\",\\n          image: \\\"/org_cover.png\\\",\\n          isSection: true\\n        };\\n        break;\\n      case 'clues':\\n        featuredItem = {\\n          title: \\\"Pistas e Mistérios\\\",\\n          author: \\\"A Verdade Oculta\\\",\\n          category: \\\"Evidências e Teorias\\\",\\n          description: \\\"Investigue os vestígios deixados por \n<truncated 4220 bytes>"

========================

Step 229 - App.jsx:
"import { useState, useEffect } from 'react';\nimport { Home, Users, Map, Search, BookOpen, Settings, Key, Building, Image as ImageIcon } from 'lucide-react';\nimport DetailModal from './components/DetailModal';\nimport AdminPanel from './components/AdminPanel';\n\nfunction App() {\n  const [activeTab, setActiveTab] = useState('home');\n  const [searchQuery, setSearchQuery] = useState('');\n  const [selectedItem, setSelectedItem] = useState(null);\n  const [db, setDb] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  const fetchData = async () => {\n    try {\n      const res = await fetch('http://localhost:3001/api/data');\n      const data = await res.json();\n      setDb(data);\n      setLoading(false);\n    } catch (err) {\n      console.error('Erro ao buscar dados. O servidor node está rodando?', err);\n      setLoading(false);\n    }\n  };\n\n  useEffect(() => {\n    fetchData();\n  }, []);\n\n  const handleUpdateData = async (newData) => {\n    setDb(newData);\n    try {\n      await fetch('http://localhost:3001/api/data', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify(newData)\n      });\n    } catch (err) {\n      console.error('Erro ao salvar no servidor.', err);\n    }\n  };\n\n  if (loading || !db) return <div style={{color:'white', padding:'3rem', textAlign:'center'}}>Carregando Universo...</div>;\n\n  const allItems = [...(db.characters || []), ...(db.locations || []), ...(db.clues || []), ...(db.organizations || [])];\n  const allPosts = [...(db.posts || [])].reverse(); // Reverse to show newest first\n  \n  // Aggregate all gallery images globally\n  const globalGallery = allItems.flatMap(item => item.gallery || []).reverse(); \n\n  const filteredItems = allItems.filter(item => {\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\n    if (activeTab === 'locations' && item.type !== 'local') return false;\n    if (activeTab === 'clues' && item.type !== 'pista') return false;\n    
<truncated 10219 bytes>

========================

Step 260 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":141,\"ReplacementContent\":\"                {showBookCover ? (\\n                  <>\\n                    {/* Explore o Universo */}\\n                    <section className=\\\"list-section\\\" style={{ marginBottom: '4rem' }}>\\n                      <h3 style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>\\n                        <Map size={18} /> EXPLORAR O UNIVERSO\\n                      </h3>\\n                      <div className=\\\"cards-grid\\\">\\n                        {similarItems.map(item => (\\n                          <div key={item.id} className=\\\"item-card\\\" onClick={() => setSelectedItem(item)}>\\n                            <img src={item.image} alt={item.name} />\\n                            <h4>{item.name}</h4>\\n                            <p>{item.role || item.territory || item.type}</p>\\n                          </div>\\n                        ))}\\n                      </div>\\n                    </section>\\n\\n                    {/* Blog Section on Home */}\",\"StartLine\":140,\"TargetContent\":\"                {showBookCover ? (\\n                  <>\\n                    {/* Blog Section on Home */}\"}]"

========================

Step 284 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":4,\"ReplacementContent\":\"import DetailModal from './components/DetailModal';\\nimport AdminPanel from './components/AdminPanel';\\nimport Reader from './components/Reader';\",\"StartLine\":3,\"TargetContent\":\"import DetailModal from './components/DetailModal';\\nimport AdminPanel from './components/AdminPanel';\"},{\"AllowMultiple\":false,\"EndLine\":9,\"ReplacementContent\":\"  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [isReading, setIsReading] = useState(false);\\n  const [db, setDb] = useState(null);\",\"StartLine\":7,\"TargetContent\":\"  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [db, setDb] = useState(null);\"},{\"AllowMultiple\":false,\"EndLine\":104,\"ReplacementContent\":\"  return (\\n    <div className=\\\"app-container\\\">\\n      {isReading && (\\n        <Reader chapters={db.chapters || []} onClose={() => setIsReading(false)} />\\n      )}\\n\\n      {/* Sidebar */}\",\"StartLine\":101,\"TargetContent\":\"  return (\\n    <div className=\\\"app-container\\\">\\n      {/* Sidebar */}\"},{\"AllowMultiple\":false,\"EndLine\":151,\"ReplacementContent\":\"                    <div className=\\\"featured-actions\\\">\\n                      {showBookCover ? (\\n                        <button className=\\\"btn-primary\\\" onClick={() => setIsReading(true)}>\\n                          <BookOpen size={18} /> LER LIVRO\\n                        </button>\\n                      ) : featuredItem.isSection ? (\",\"StartLine\":146,\"TargetContent\":\"                    <div className=\\\"featured-actions\\\">\\n                      {showBookCover ? (\\n                        <button className=\\\"btn-primary\\\" onClick={() => setActiveTab('characters')}>\\n                          <BookOpen size={18} /> LER PERSONAGENS\\n                        </button>\\n                      ) : featuredItem.isSection ? (\"}]"

========================

Step 533 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":25,\"ReplacementContent\":\"function App() {\\n  const [activeTab, setActiveTab] = useState('home');\\n  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [isReading, setIsReading] = useState(null);\\n  const [selectedSagaBook, setSelectedSagaBook] = useState(null);\\n  const [db, setDb] = useState(null);\\n  const [loading, setLoading] = useState(true);\\n\\n  const fetchData = async () => {\\n    try {\\n      const res = await fetch('http://localhost:3001/api/data');\\n      let data = await res.json();\\n      \\n      // Auto-Migration: Convert single book to books collection\\n      if (data.book && data.chapters) {\\n        if (!data.books) data.books = [];\\n        data.books.push({\\n          id: \\\"book_1\\\",\\n          ...data.book,\\n          chapters: data.chapters\\n        });\\n        delete data.book;\\n        delete data.chapters;\\n        \\n        await fetch('http://localhost:3001/api/data', {\\n          method: 'POST',\\n          headers: { 'Content-Type': 'application/json' },\\n          body: JSON.stringify(data)\\n        });\\n      }\\n\\n      setDb(data);\\n      setLoading(false);\\n    } catch (err) {\\n      console.error('Erro ao buscar dados. O servidor node está rodando?', err);\\n      setLoading(false);\\n    }\\n  };\",\"StartLine\":7,\"TargetContent\":\"function App() {\\n  const [activeTab, setActiveTab] = useState('home');\\n  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [isReading, setIsReading] = useState(false);\\n  const [db, setDb] = useState(null);\\n  const [loading, setLoading] = useState(true);\\n\\n  const fetchData = async () => {\\n    try {\\n      const res = await fetch('http://localhost:3001/api/data');\\n      const data = await res.json();\\n      setDb(data);\\n      setLoading(false);\\n    } catch (err) {\\n      console.error('Erro ao buscar dados. O servidor node está rodando?', err);\\n      setLoading(false);\\n    }\\n \n<truncated 3244 bytes>"

========================

Step 586 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":82,\"ReplacementContent\":\"  const getSearchKeywords = (query) => {\\n    const stopWords = ['quem', 'é', 'o', 'a', 'os', 'as', 'mais', 'de', 'todos', 'dos', 'das', 'personagens', 'personagem', 'qual', 'como', 'onde', 'por', 'que', 'um', 'uma'];\\n    const words = query.toLowerCase().replace(/[?.,!]/g, '').split(/\\\\s+/);\\n    return words.filter(w => w.length > 2 && !stopWords.includes(w));\\n  };\\n\\n  const filteredItems = allItems.filter(item => {\\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\\n    if (activeTab === 'locations' && item.type !== 'local') return false;\\n    if (activeTab === 'clues' && item.type !== 'pista') return false;\\n    if (activeTab === 'organizations' && item.type !== 'organizacao') return false;\\n    \\n    if (searchQuery) {\\n      const keywords = getSearchKeywords(searchQuery);\\n      if (keywords.length === 0) {\\n        return item.name.toLowerCase().includes(searchQuery.toLowerCase());\\n      }\\n      \\n      const itemText = `\\n        ${item.name || ''} \\n        ${item.role || ''} \\n        ${item.territory || ''} \\n        ${item.description || ''} \\n        ${item.motivations || ''} \\n        ${item.curiosities || ''} \\n        ${item.reality || ''} \\n        ${item.found || ''}\\n      `.toLowerCase();\\n      \\n      // Busca semântica local: Todas as palavras-chave devem estar no texto\\n      return keywords.every(kw => itemText.includes(kw));\\n    }\\n    return true;\\n  });\",\"StartLine\":72,\"TargetContent\":\"  const filteredItems = allItems.filter(item => {\\n    if (activeTab === 'characters' && item.type !== 'personagem') return false;\\n    if (activeTab === 'locations' && item.type !== 'local') return false;\\n    if (activeTab === 'clues' && item.type !== 'pista') return false;\\n    if (activeTab === 'organizations' && item.type !== 'organizacao') return false;\\n    if (searchQuery) {\\n      return item.name.toLowerCase().includes(searchQuery.toLowerCase()) || \\n             item.role?.toLowerCase().includes(s\n<truncated 61 bytes>"

========================

Step 606 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":3,\"ReplacementContent\":\"import Dossier from './components/Dossier';\",\"StartLine\":3,\"TargetContent\":\"import DetailModal from './components/DetailModal';\"},{\"AllowMultiple\":false,\"EndLine\":192,\"ReplacementContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\">\\n        {activeTab === 'admin' ? (\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\\n        ) : selectedItem ? (\\n          <Dossier item={selectedItem} onClose={() => setSelectedItem(null)} />\\n        ) : (\\n          <>\\n            <div className=\\\"search-container\\\">\",\"StartLine\":186,\"TargetContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\">\\n        {activeTab === 'admin' ? (\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\\n        ) : (\\n          <>\\n            <div className=\\\"search-container\\\">\"},{\"AllowMultiple\":false,\"EndLine\":338,\"ReplacementContent\":\"    </div>\\n  );\\n}\",\"StartLine\":332,\"TargetContent\":\"      {/* Modal */}\\n      {selectedItem && (\\n        <DetailModal item={selectedItem} onClose={() => setSelectedItem(null)} />\\n      )}\\n    </div>\\n  );\\n}\"}]"

========================

Step 636 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":14,\"ReplacementContent\":\"import Reader from './components/Reader';\\nimport NetflixHome from './components/NetflixHome';\\n\\nfunction App() {\\n  const [activeTab, setActiveTab] = useState('home');\\n  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [isReading, setIsReading] = useState(null);\\n  const [selectedSagaBook, setSelectedSagaBook] = useState(null);\\n  const [currentUniverse, setCurrentUniverse] = useState(null);\\n  const [db, setDb] = useState(null);\",\"StartLine\":5,\"TargetContent\":\"import Reader from './components/Reader';\\n\\nfunction App() {\\n  const [activeTab, setActiveTab] = useState('home');\\n  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [isReading, setIsReading] = useState(null);\\n  const [selectedSagaBook, setSelectedSagaBook] = useState(null);\\n  const [db, setDb] = useState(null);\"},{\"AllowMultiple\":false,\"EndLine\":39,\"ReplacementContent\":\"      // Auto-Migration: Convert single book to books collection\\n      let needsSave = false;\\n      if (data.book && data.chapters) {\\n        if (!data.books) data.books = [];\\n        data.books.push({\\n          id: \\\"book_1\\\",\\n          ...data.book,\\n          chapters: data.chapters\\n        });\\n        delete data.book;\\n        delete data.chapters;\\n        needsSave = true;\\n      }\\n      \\n      // Auto-Migration: Isolate universes\\n      const listsToMigrate = ['characters', 'locations', 'organizations', 'clues', 'posts'];\\n      listsToMigrate.forEach(listName => {\\n        if (data[listName]) {\\n          data[listName] = data[listName].map(item => {\\n            if (!item.bookId) {\\n              needsSave = true;\\n              return { ...item, bookId: data.books && data.books.length > 0 ? data.books[0].id : 'book_1' };\\n            }\\n            return item;\\n          });\\n        }\\n      });\\n\\n      if (needsSave) {\\n        await fetch('http://localhost:30\n<truncated 3376 bytes>"

========================

Step 669 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":170,\"ReplacementContent\":\"      default:\\n        featuredItem = selectedSagaBook || (db.books || []).find(b => b.id === currentUniverse) || (db.books && db.books.length > 0 ? db.books[0] : null);\\n    }\\n  }\",\"StartLine\":167,\"TargetContent\":\"      default:\\n        featuredItem = selectedSagaBook || (db.books && db.books.length > 0 ? db.books[0] : null);\\n    }\\n  }\"},{\"AllowMultiple\":false,\"EndLine\":184,\"ReplacementContent\":\"        <NetflixHome \\n          books={db.books || []} \\n          onExplore={(id) => { setCurrentUniverse(id); setActiveTab('home'); }} \\n          onRead={(book) => setIsReading(book)} \\n        />\",\"StartLine\":180,\"TargetContent\":\"        <NetflixHome \\n          books={db.books || []} \\n          onExplore={(id) => { setCurrentUniverse(id); setActiveTab('characters'); }} \\n          onRead={(book) => setIsReading(book)} \\n        />\"},{\"AllowMultiple\":false,\"EndLine\":209,\"ReplacementContent\":\"      {/* Sidebar */}\\n      <aside className=\\\"sidebar\\\">\\n        <div \\n          className=\\\"sidebar-logo\\\" \\n          onClick={() => { setCurrentUniverse(null); setActiveTab('home'); }} \\n          title=\\\"Voltar para SAGAFLIX\\\"\\n          style={{ cursor: 'pointer' }}\\n        >\\n          <BookOpen size={28} color=\\\"var(--accent-gold)\\\" />\\n        </div>\\n        <div className=\\\"nav-links\\\">\\n          <div className={`nav-item ${activeTab === 'home' ? 'active' : ''}`} onClick={() => setActiveTab('home')} title=\\\"Início do Universo\\\">\\n            <Home size={24} />\\n          </div>\",\"StartLine\":201,\"TargetContent\":\"      {/* Sidebar */}\\n      <aside className=\\\"sidebar\\\">\\n        <div className=\\\"sidebar-logo\\\">\\n          <BookOpen size={28} />\\n        </div>\\n        <div className=\\\"nav-links\\\">\\n          <div className=\\\"nav-item\\\" onClick={() => { setCurrentUniverse(null); setActiveTab('home'); }} title=\\\"Voltar para SAGAFLIX\\\">\\n            <Home size={24} />\\n          </div>\"}]"

========================

Step 749 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":193,\"ReplacementContent\":\"  if (!currentUniverse && activeTab !== 'admin') {\\n    return (\\n      <div className=\\\"app-container\\\">\\n        {isReading && (\\n          <Reader chapters={isReading.chapters || []} onClose={() => setIsReading(null)} />\\n        )}\\n        {showLoginModal && (\\n          <LoginModal \\n            onClose={() => setShowLoginModal(false)} \\n            onLogin={handleLogin} \\n            users={db.users || []} \\n          />\\n        )}\\n        <NetflixHome \\n          books={db.books || []} \\n          onExplore={(id) => { setCurrentUniverse(id); setActiveTab('home'); }} \\n          onRead={(book) => setIsReading(book)} \\n          user={currentUser}\\n          onLoginClick={() => setShowLoginModal(true)}\\n          onLogout={() => setCurrentUser(null)}\\n        />\\n        <button \\n          style={{ position: 'fixed', bottom: '1rem', right: '1rem', background: 'transparent', border: 'none', color: 'rgba(255,255,255,0.1)', cursor: 'pointer', zIndex: 10000 }} \\n          onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\"\\n        >\\n          <Settings size={14} />\\n        </button>\\n      </div>\\n    );\\n  }\",\"StartLine\":174,\"TargetContent\":\"  if (!currentUniverse && activeTab !== 'admin') {\\n    return (\\n      <div className=\\\"app-container\\\">\\n        {isReading && (\\n          <Reader chapters={isReading.chapters || []} onClose={() => setIsReading(null)} />\\n        )}\\n        <NetflixHome \\n          books={db.books || []} \\n          onExplore={(id) => { setCurrentUniverse(id); setActiveTab('home'); }} \\n          onRead={(book) => setIsReading(book)} \\n        />\\n        <button \\n          style={{ position: 'fixed', bottom: '1rem', right: '1rem', background: 'transparent', border: 'none', color: 'rgba(255,255,255,0.1)', cursor: 'pointer', zIndex: 10000 }} \\n          onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\"\\n        >\\n          <Settings size={14} />\\n        </button>\\n      </div>\\n    );\\n  }\"}]"

========================

Step 752 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":16,\"ReplacementContent\":\"import NetflixHome from './components/NetflixHome';\\nimport LoginModal from './components/LoginModal';\\n\\nfunction App() {\\n  const [activeTab, setActiveTab] = useState('home');\\n  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [isReading, setIsReading] = useState(null);\\n  const [selectedSagaBook, setSelectedSagaBook] = useState(null);\\n  const [currentUniverse, setCurrentUniverse] = useState(null);\\n  const [currentUser, setCurrentUser] = useState(null);\\n  const [showLoginModal, setShowLoginModal] = useState(false);\\n  const [db, setDb] = useState(null);\\n  const [loading, setLoading] = useState(true);\\n\\n  const fetchData = async () => {\",\"StartLine\":6,\"TargetContent\":\"import NetflixHome from './components/NetflixHome';\\n\\nfunction App() {\\n  const [activeTab, setActiveTab] = useState('home');\\n  const [searchQuery, setSearchQuery] = useState('');\\n  const [selectedItem, setSelectedItem] = useState(null);\\n  const [isReading, setIsReading] = useState(null);\\n  const [selectedSagaBook, setSelectedSagaBook] = useState(null);\\n  const [currentUniverse, setCurrentUniverse] = useState(null);\\n  const [db, setDb] = useState(null);\\n  const [loading, setLoading] = useState(true);\\n\\n  const fetchData = async () => {\"},{\"AllowMultiple\":false,\"EndLine\":58,\"ReplacementContent\":\"      // Auto-Migration: Isolate universes\\n      const listsToMigrate = ['characters', 'locations', 'organizations', 'clues', 'posts'];\\n      listsToMigrate.forEach(listName => {\\n        if (data[listName]) {\\n          data[listName] = data[listName].map(item => {\\n            if (!item.bookId) {\\n              needsSave = true;\\n              return { ...item, bookId: data.books && data.books.length > 0 ? data.books[0].id : 'book_1' };\\n            }\\n            return item;\\n          });\\n        }\\n      });\\n\\n      if (!data.users) {\\n        data.users = [];\\n        needsSave = true;\\n      }\\n\\n      i\n<truncated 1909 bytes>"

========================

Step 776 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":16,\"ReplacementContent\":\"  const [currentUniverse, setCurrentUniverse] = useState(null);\\n  const [currentUser, setCurrentUser] = useState(null);\\n  const [showLoginModal, setShowLoginModal] = useState(false);\\n  const [loginType, setLoginType] = useState('reader'); // 'reader' or 'author'\\n  const [db, setDb] = useState(null);\",\"StartLine\":12,\"TargetContent\":\"  const [currentUniverse, setCurrentUniverse] = useState(null);\\n  const [currentUser, setCurrentUser] = useState(null);\\n  const [showLoginModal, setShowLoginModal] = useState(false);\\n  const [db, setDb] = useState(null);\"},{\"AllowMultiple\":false,\"EndLine\":85,\"ReplacementContent\":\"  const handleLogin = (userObj, isNew) => {\\n    if (isNew) {\\n      // First user registered via 'admin' email bypass or default logic\\n      const isAdmin = userObj.email === 'wagner@sagaflix.com' || userObj.email === 'admin@admin.com';\\n      if (isAdmin) {\\n        userObj.role = 'admin';\\n        userObj.status = 'approved';\\n      }\\n      const newDb = { ...db, users: [...(db.users || []), userObj] };\\n      handleUpdateData(newDb);\\n    }\\n    setCurrentUser(userObj);\\n    setShowLoginModal(false);\\n  };\",\"StartLine\":78,\"TargetContent\":\"  const handleLogin = (userObj, isNew) => {\\n    if (isNew) {\\n      const newDb = { ...db, users: [...(db.users || []), userObj] };\\n      handleUpdateData(newDb);\\n    }\\n    setCurrentUser(userObj);\\n    setShowLoginModal(false);\\n  };\"},{\"AllowMultiple\":false,\"EndLine\":186,\"ReplacementContent\":\"        {showLoginModal && (\\n          <LoginModal \\n            type={loginType}\\n            onClose={() => setShowLoginModal(false)} \\n            onLogin={handleLogin} \\n            users={db.users || []} \\n          />\\n        )}\\n        <NetflixHome \\n          books={db.books || []} \\n          onExplore={(id) => { setCurrentUniverse(id); setActiveTab('home'); }} \\n          onRead={(book) => setIsReading(book)} \\n          user={currentUser}\\n          onLoginClick={(type) => { setLoginType(type); se\n<truncated 1113 bytes>"

========================

Step 794 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":250,\"ReplacementContent\":\"      {/* Admin Panel */}\\n      {activeTab === 'admin' && (\\n        <section className=\\\"list-section\\\">\\n          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>\\n            <h2>Painel {currentUser?.role === 'admin' ? 'Master' : 'do Autor'}</h2>\\n          </div>\\n          {(!currentUser || currentUser.role === 'reader') ? (\\n            <div style={{ padding: '2rem', textAlign: 'center', color: 'var(--text-muted)' }}>\\n              Acesso restrito. Faça login como Autor ou Admin para acessar o CMS.\\n            </div>\\n          ) : (\\n            <AdminPanel data={db} onUpdate={handleUpdateData} currentUser={currentUser} />\\n          )}\\n        </section>\\n      )}\",\"StartLine\":239,\"TargetContent\":\"      {/* Admin Panel */}\\n      {activeTab === 'admin' && (\\n        <section className=\\\"list-section\\\">\\n          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>\\n            <h2>Painel CMS</h2>\\n          </div>\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\\n        </section>\\n      )}\"}]"

========================

Step 803 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":273,\"ReplacementContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\">\\n        {activeTab === 'admin' ? (\\n          (!currentUser || currentUser.role === 'reader') ? (\\n            <div style={{ padding: '4rem', textAlign: 'center', color: 'var(--text-muted)' }}>\\n              Acesso restrito. Faça login como Autor ou Administrador.\\n            </div>\\n          ) : (\\n            <AdminPanel data={db} onUpdate={handleUpdateData} currentUser={currentUser} />\\n          )\\n        ) : selectedItem ? (\\n          <Dossier item={selectedItem} onClose={() => setSelectedItem(null)} />\",\"StartLine\":268,\"TargetContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\">\\n        {activeTab === 'admin' ? (\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\\n        ) : selectedItem ? (\\n          <Dossier item={selectedItem} onClose={() => setSelectedItem(null)} />\"}]"

========================

Step 909 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":110,\"ReplacementContent\":\"  if (loading || !db) return <div style={{color:'white', padding:'3rem', textAlign:'center'}}>Carregando Universo...</div>;\\n\\n  const visibleBooks = (db.books || []).filter(b => {\\n    if (currentUser?.role === 'admin') return true;\\n    if (currentUser?.role === 'author' && b.authorId === currentUser.id) return true;\\n    return !b.status || b.status === 'approved';\\n  });\\n\\n  const allItems = [...(db.characters || []), ...(db.locations || []), ...(db.clues || []), ...(db.organizations || [])];\",\"StartLine\":108,\"TargetContent\":\"  if (loading || !db) return <div style={{color:'white', padding:'3rem', textAlign:'center'}}>Carregando Universo...</div>;\\n\\n  const allItems = [...(db.characters || []), ...(db.locations || []), ...(db.clues || []), ...(db.organizations || [])];\"},{\"AllowMultiple\":false,\"EndLine\":162,\"ReplacementContent\":\"      case 'home':\\n        featuredItem = selectedSagaBook || (visibleBooks.length > 0 ? visibleBooks[0] : null);\\n        break;\",\"StartLine\":160,\"TargetContent\":\"      case 'home':\\n        featuredItem = selectedSagaBook || (db.books && db.books.length > 0 ? db.books[0] : null);\\n        break;\"},{\"AllowMultiple\":false,\"EndLine\":193,\"ReplacementContent\":\"      default:\\n        featuredItem = selectedSagaBook || visibleBooks.find(b => b.id === currentUniverse) || (visibleBooks.length > 0 ? visibleBooks[0] : null);\\n    }\",\"StartLine\":191,\"TargetContent\":\"      default:\\n        featuredItem = selectedSagaBook || (db.books || []).find(b => b.id === currentUniverse) || (db.books && db.books.length > 0 ? db.books[0] : null);\\n    }\"},{\"AllowMultiple\":false,\"EndLine\":213,\"ReplacementContent\":\"        <NetflixHome \\n          books={visibleBooks} \",\"StartLine\":212,\"TargetContent\":\"        <NetflixHome \\n          books={db.books || []} \"}]"

========================

Step 930 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":58,\"ReplacementContent\":\"      if (!data.users) {\\n        data.users = [];\\n        needsSave = true;\\n      }\\n\\n      if (!data.users.find(u => u.email === 'wagner@sagaflix.com')) {\\n        data.users.push({\\n          id: 'admin_master',\\n          name: 'Wagner',\\n          email: 'wagner@sagaflix.com',\\n          password: 'admin',\\n          role: 'admin',\\n          status: 'approved'\\n        });\\n        needsSave = true;\\n      }\",\"StartLine\":55,\"TargetContent\":\"      if (!data.users) {\\n        data.users = [];\\n        needsSave = true;\\n      }\"}]"

========================

Step 939 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":244,\"ReplacementContent\":\"        {(currentUser?.role === 'admin' || currentUser?.role === 'author') && (\\n          <button \\n            style={{ position: 'fixed', bottom: '1rem', right: '1rem', background: 'transparent', border: 'none', color: 'rgba(255,255,255,0.1)', cursor: 'pointer', zIndex: 10000 }} \\n            onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\"\\n          >\\n            <Settings size={14} />\\n          </button>\\n        )}\",\"StartLine\":238,\"TargetContent\":\"        <button \\n          style={{ position: 'fixed', bottom: '1rem', right: '1rem', background: 'transparent', border: 'none', color: 'rgba(255,255,255,0.1)', cursor: 'pointer', zIndex: 10000 }} \\n          onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\"\\n        >\\n          <Settings size={14} />\\n        </button>\"},{\"AllowMultiple\":false,\"EndLine\":282,\"ReplacementContent\":\"          {(currentUser?.role === 'admin' || currentUser?.role === 'author') && (\\n            <div className={`nav-item ${activeTab === 'admin' ? 'active' : ''}`} onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\" style={{marginTop: 'auto', marginBottom: '2rem'}}>\\n              <Settings size={24} />\\n            </div>\\n          )}\",\"StartLine\":280,\"TargetContent\":\"          <div className={`nav-item ${activeTab === 'admin' ? 'active' : ''}`} onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\" style={{marginTop: 'auto', marginBottom: '2rem'}}>\\n            <Settings size={24} />\\n          </div>\"}]"

========================

Step 962 - App.jsx:
"import { useState, useEffect } from 'react';\nimport AdminPanel from './components/AdminPanel';\nimport { BookOpen, Lock } from 'lucide-react';\n\nexport default function AdminApp() {\n  const [db, setDb] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [currentUser, setCurrentUser] = useState(null);\n  const [loginEmail, setLoginEmail] = useState('');\n  const [loginPassword, setLoginPassword] = useState('');\n  const [loginError, setLoginError] = useState('');\n\n  const fetchData = async () => {\n    try {\n      const response = await fetch('http://localhost:3001/api/data');\n      let data = await response.json();\n      \n      if (!data.users) {\n        data.users = [];\n      }\n      setDb(data);\n      setLoading(false);\n    } catch (err) {\n      console.error('Erro ao buscar dados.', err);\n      setLoading(false);\n    }\n  };\n\n  useEffect(() => {\n    fetchData();\n  }, []);\n\n  const handleUpdateData = async (newData) => {\n    setDb(newData);\n    try {\n      await fetch('http://localhost:3001/api/data', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify(newData)\n      });\n    } catch (err) {\n      console.error('Erro ao salvar no servidor.', err);\n    }\n  };\n\n  const handleLogin = (e) => {\n    e.preventDefault();\n    setLoginError('');\n    \n    if (!db || !db.users) return;\n    \n    const user = db.users.find(u => u.email === loginEmail && u.password === loginPassword);\n    \n    if (user) {\n      if (user.role === 'admin' || user.role === 'author') {\n        setCurrentUser(user);\n      } else {\n        setLoginError('Acesso Negado: Você não tem privilégios administrativos.');\n      }\n    } else {\n      setLoginError('Credenciais inválidas.');\n    }\n  };\n\n  if (loading) {\n    return <div style={{ background: '#121212', height: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'white' }}>Carregando Painel...</div>;\n  }\n\n  if (!currentUser
<truncated 3062 bytes>

========================

Step 974 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":2,\"ReplacementContent\":\"import { useState, useEffect } from 'react';\",\"StartLine\":1,\"TargetContent\":\"import { useState, useEffect } from 'react';\\nimport AdminPanel from './components/AdminPanel';\"},{\"AllowMultiple\":false,\"EndLine\":304,\"ReplacementContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\">\\n        {selectedItem ? (\\n          <Dossier item={selectedItem} onClose={() => setSelectedItem(null)} />\\n        ) : (\\n          <>\\n            <div className=\\\"search-container\\\">\",\"StartLine\":290,\"TargetContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\">\\n        {activeTab === 'admin' ? (\\n          (!currentUser || currentUser.role === 'reader') ? (\\n            <div style={{ padding: '4rem', textAlign: 'center', color: 'var(--text-muted)' }}>\\n              Acesso restrito. Faça login como Autor ou Administrador.\\n            </div>\\n          ) : (\\n            <AdminPanel data={db} onUpdate={handleUpdateData} currentUser={currentUser} />\\n          )\\n        ) : selectedItem ? (\\n          <Dossier item={selectedItem} onClose={() => setSelectedItem(null)} />\\n        ) : (\\n          <>\\n            <div className=\\\"search-container\\\">\"}]"

========================

Step 977 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":242,\"ReplacementContent\":\"        {(currentUser?.role === 'admin' || currentUser?.role === 'author') && (\\n          <a \\n            href=\\\"/admin\\\"\\n            style={{ position: 'fixed', bottom: '1rem', right: '1rem', background: 'transparent', border: 'none', color: 'rgba(255,255,255,0.1)', cursor: 'pointer', zIndex: 10000 }} \\n            title=\\\"Painel CMS\\\"\\n          >\\n            <Settings size={14} />\\n          </a>\\n        )}\",\"StartLine\":235,\"TargetContent\":\"        {(currentUser?.role === 'admin' || currentUser?.role === 'author') && (\\n          <button \\n            style={{ position: 'fixed', bottom: '1rem', right: '1rem', background: 'transparent', border: 'none', color: 'rgba(255,255,255,0.1)', cursor: 'pointer', zIndex: 10000 }} \\n            onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\"\\n          >\\n            <Settings size={14} />\\n          </button>\\n        )}\"},{\"AllowMultiple\":false,\"EndLine\":282,\"ReplacementContent\":\"          {(currentUser?.role === 'admin' || currentUser?.role === 'author') && (\\n            <a href=\\\"/admin\\\" className=\\\"nav-item\\\" title=\\\"Painel CMS\\\" style={{marginTop: 'auto', marginBottom: '2rem', display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'inherit', textDecoration: 'none'}}>\\n              <Settings size={24} />\\n            </a>\\n          )}\",\"StartLine\":278,\"TargetContent\":\"          {(currentUser?.role === 'admin' || currentUser?.role === 'author') && (\\n            <div className={`nav-item ${activeTab === 'admin' ? 'active' : ''}`} onClick={() => setActiveTab('admin')} title=\\\"Painel CMS\\\" style={{marginTop: 'auto', marginBottom: '2rem'}}>\\n              <Settings size={24} />\\n            </div>\\n          )}\"}]"

========================

Step 996 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":26,\"ReplacementContent\":\"      if (!data.users) {\\n        data.users = [];\\n      }\\n      \\n      let needsSave = false;\\n      let wagnerUser = data.users.find(u => u.email === 'wagner@sagaflix.com');\\n      if (!wagnerUser) {\\n        data.users.push({\\n          id: 'admin_master',\\n          name: 'Wagner',\\n          email: 'wagner@sagaflix.com',\\n          password: 'admin',\\n          role: 'admin',\\n          status: 'approved'\\n        });\\n        needsSave = true;\\n      } else if (wagnerUser.password !== 'admin' || wagnerUser.role !== 'admin') {\\n        wagnerUser.password = 'admin';\\n        wagnerUser.role = 'admin';\\n        needsSave = true;\\n      }\\n\\n      setDb(data);\\n      setLoading(false);\\n\\n      if (needsSave) {\\n        await fetch('http://localhost:3001/api/data', {\\n          method: 'POST',\\n          headers: { 'Content-Type': 'application/json' },\\n          body: JSON.stringify(data)\\n        });\\n      }\\n    } catch (err) {\\n      console.error('Erro ao buscar dados.', err);\\n      setLoading(false);\\n    }\",\"StartLine\":18,\"TargetContent\":\"      if (!data.users) {\\n        data.users = [];\\n      }\\n      setDb(data);\\n      setLoading(false);\\n    } catch (err) {\\n      console.error('Erro ao buscar dados.', err);\\n      setLoading(false);\\n    }\"}]"

========================

Step 1105 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":43,\"ReplacementContent\":\"      let needsSave = false;\\n      \\n      // 1. Master Admin\\n      let wagnerUser = data.users.find(u => u.email === 'wagner@sagaflix.com');\\n      if (!wagnerUser) {\\n        data.users.push({ id: 'admin_master', name: 'Wagner (Master Admin)', email: 'wagner@sagaflix.com', password: 'admin', role: 'admin', status: 'approved' });\\n        needsSave = true;\\n      } else if (wagnerUser.password !== 'admin' || wagnerUser.role !== 'admin') {\\n        wagnerUser.password = 'admin'; wagnerUser.role = 'admin'; needsSave = true;\\n      }\\n\\n      // 2. Autor (O próprio Wagner)\\n      let wagnerAutor = data.users.find(u => u.email === 'autor.wagner@sagaflix.com');\\n      if (!wagnerAutor) {\\n         data.users.push({ id: 'autor_wagner', name: 'Wagner Alves (Autor)', email: 'autor.wagner@sagaflix.com', password: 'autor', role: 'author', status: 'approved', bio: 'Autor de Jardim das Flores' });\\n         needsSave = true;\\n      }\\n\\n      // 3. Leitor 01\\n      let leitor = data.users.find(u => u.email === 'leitor01@sagaflix.com');\\n      if (!leitor) {\\n         data.users.push({ id: 'leitor_01', name: 'leitor01', email: 'leitor01@sagaflix.com', password: '123', role: 'reader', status: 'approved' });\\n         needsSave = true;\\n      }\\n\\n      setDb(data);\\n      setLoading(false);\\n\\n      if (needsSave) {\",\"StartLine\":22,\"TargetContent\":\"      let needsSave = false;\\n      let wagnerUser = data.users.find(u => u.email === 'wagner@sagaflix.com');\\n      if (!wagnerUser) {\\n        data.users.push({\\n          id: 'admin_master',\\n          name: 'Wagner',\\n          email: 'wagner@sagaflix.com',\\n          password: 'admin',\\n          role: 'admin',\\n          status: 'approved'\\n        });\\n        needsSave = true;\\n      } else if (wagnerUser.password !== 'admin' || wagnerUser.role !== 'admin') {\\n        wagnerUser.password = 'admin';\\n        wagnerUser.role = 'admin';\\n        needsSave = true;\\n      }\\n\\n      setDb(data);\\n      setLoading(fals\n<truncated 32 bytes>"

========================

Step 1138 - App.jsx:
"  const visibleBooks = (db.books || []).filter(b => {\n    if (currentUser?.role === 'admin') return true;\n    if (currentUser?.role === 'author' && b.authorId === currentUser.id) return true;\n    return !b.status || b.status === 'approved' || b.status === 'PUBLISHED';\n  });"

========================

Step 1150 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":125,\"ReplacementContent\":\"  const visibleBooks = (db.books || []).filter(b => {\\n    if (currentUser?.role === 'admin') return true;\\n    if (currentUser?.role === 'author' && b.authorId === currentUser.id) return true;\\n    return b.status === 'PUBLISHED';\\n  });\",\"StartLine\":121,\"TargetContent\":\"  const visibleBooks = (db.books || []).filter(b => {\\n    if (currentUser?.role === 'admin') return true;\\n    if (currentUser?.role === 'author' && b.authorId === currentUser.id) return true;\\n    return !b.status || b.status === 'approved' || b.status === 'PUBLISHED';\\n  });\"}]"

========================

Step 291 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":4,\"ReplacementContent\":\"import { Home, Users, Map, Search, BookOpen, Settings, Key, Building, Image as ImageIcon } from 'lucide-react';\\nimport DetailModal from './components/DetailModal';\\nimport AdminPanel from './components/AdminPanel';\\nimport Reader from './components/Reader';\",\"StartLine\":2,\"TargetContent\":\"import { Home, Users, Map, Search, BookOpen, Settings, Key, Building, Image as ImageIcon } from 'lucide-react';\\nimport DetailModal from './components/DetailModal';\\nimport AdminPanel from './components/AdminPanel';\"},{\"AllowMultiple\":false,\"EndLine\":167,\"ReplacementContent\":\"                      {showBookCover ? (\\n                        <button className=\\\"btn-primary\\\" onClick={() => setActiveTab('reader')}>\\n                          <BookOpen size={18} /> LER LIVRO\\n                        </button>\\n                      ) : featuredItem.isSection ? (\",\"StartLine\":163,\"TargetContent\":\"                      {showBookCover ? (\\n                        <button className=\\\"btn-primary\\\" onClick={() => setActiveTab('characters')}>\\n                          <BookOpen size={18} /> LER PERSONAGENS\\n                        </button>\\n                      ) : featuredItem.isSection ? (\"},{\"AllowMultiple\":false,\"EndLine\":134,\"ReplacementContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\" style={{ padding: activeTab === 'reader' ? '0' : '2rem 3rem' }}>\\n        {activeTab === 'reader' ? (\\n          <Reader data={db} onClose={() => setActiveTab('home')} />\\n        ) : activeTab === 'admin' ? (\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\\n        ) : (\\n          <>\",\"StartLine\":133,\"TargetContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\">\\n        {activeTab === 'admin' ? (\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\\n        ) : (\\n          <>\"}]"

========================

Step 316 - App.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":137,\"ReplacementContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\" style={{ padding: (activeTab === 'reader' || activeTab === 'admin') ? '0' : '2rem 3rem', height: '100%', overflow: 'hidden' }}>\\n        {activeTab === 'reader' ? (\\n          <Reader data={db} onClose={() => setActiveTab('home')} />\\n        ) : activeTab === 'admin' ? (\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\",\"StartLine\":133,\"TargetContent\":\"      {/* Main Content */}\\n      <main className=\\\"main-content\\\" style={{ padding: activeTab === 'reader' ? '0' : '2rem 3rem' }}>\\n        {activeTab === 'reader' ? (\\n          <Reader data={db} onClose={() => setActiveTab('home')} />\\n        ) : activeTab === 'admin' ? (\\n          <AdminPanel data={db} onUpdate={handleUpdateData} />\"}]"