Step 277 - Reader.jsx:
"import { useState, useEffect } from 'react';\nimport { X, Moon, Sun, ChevronLeft, ChevronRight, Menu } from 'lucide-react';\n\nexport default function Reader({ chapters, onClose }) {\n  const [theme, setTheme] = useState('dark');\n  const [activeChapterIdx, setActiveChapterIdx] = useState(0);\n  const [activePageIdx, setActivePageIdx] = useState(0);\n  const [menuOpen, setMenuOpen] = useState(false);\n\n  // Quando o capítulo mudar, volta para a primeira página\n  useEffect(() => {\n    setActivePageIdx(0);\n  }, [activeChapterIdx]);\n\n  const chapter = chapters && chapters.length > 0 ? chapters[activeChapterIdx] : null;\n  const page = chapter && chapter.pages && chapter.pages.length > 0 ? chapter.pages[activePageIdx] : null;\n\n  const handleNextPage = () => {\n    if (chapter && activePageIdx < chapter.pages.length - 1) {\n      setActivePageIdx(activePageIdx + 1);\n    } else if (activeChapterIdx < chapters.length - 1) {\n      setActiveChapterIdx(activeChapterIdx + 1);\n    }\n  };\n\n  const handlePrevPage = () => {\n    if (activePageIdx > 0) {\n      setActivePageIdx(activePageIdx - 1);\n    } else if (activeChapterIdx > 0) {\n      setActiveChapterIdx(activeChapterIdx - 1);\n      // Vai para a última página do capítulo anterior\n      const prevChapter = chapters[activeChapterIdx - 1];\n      setActivePageIdx(prevChapter.pages ? prevChapter.pages.length - 1 : 0);\n    }\n  };\n\n  const isDark = theme === 'dark';\n  const bgColor = isDark ? '#1a1b1e' : '#fcfaf5'; // Off-white para light mode\n  const textColor = isDark ? '#e0e0e0' : '#2b2b2b';\n  const panelBg = isDark ? '#24262b' : '#f0ebd8';\n  const borderColor = isDark ? '#383b44' : '#dcd3b6';\n\n  if (!chapters || chapters.length === 0) {\n    return (\n      <div style={{ position: 'fixed', top:0, left:0, right:0, bottom:0, background: bgColor, color: textColor, zIndex: 9999, display: 'flex', alignItems: 'center', justifyContent: 'center', flexDirection: 'column' }}>\n        <h2>Nenhum capítulo disponível.</h2>\n        <p style={{ margi
<truncated 7304 bytes>

========================

Step 290 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":60,\"ReplacementContent\":\"        .reader-text { flex: 1; display: flex; flex-direction: column; justify-content: flex-start; max-width: 800px; margin: 0 auto; width: 100%; white-space: pre-wrap; padding-top: 2rem; }\\n        .reader-right { width: 30%; max-width: 450px; border-left: 1px solid ${borderColor}; background: ${bgColor}; display: flex; align-items: center; justify-content: center; padding: 2rem; }\\n        .reader-img { width: 100%; aspect-ratio: 9/16; object-fit: cover; border-radius: 8px; box-shadow: 0 10px 30px rgba(0,0,0,0.2); }\",\"StartLine\":58,\"TargetContent\":\"        .reader-text { flex: 1; display: flex; flex-direction: column; justify-content: center; max-width: 800px; margin: 0 auto; width: 100%; white-space: pre-wrap; }\\n        .reader-right { width: 30%; max-width: 450px; border-left: 1px solid ${borderColor}; background: ${bgColor}; display: flex; align-items: center; justify-content: center; padding: 2rem; }\\n        .reader-img { max-width: 100%; max-height: 100%; object-fit: contain; border-radius: 8px; box-shadow: 0 10px 30px rgba(0,0,0,0.2); }\"},{\"AllowMultiple\":false,\"EndLine\":81,\"ReplacementContent\":\"        @media (min-width: 769px) {\\n          .mobile-art { display: none; }\\n        }\\n\\n        .reader-text strong { font-weight: bold; color: var(--accent-gold); }\\n        .reader-text em { font-style: italic; }\\n        .reader-text blockquote { \\n          font-style: italic; \\n          border-left: 4px solid var(--accent-gold); \\n          padding-left: 1.5rem; \\n          margin: 1.5rem 0; \\n          color: var(--text-muted); \\n          font-family: 'Playfair Display', serif;\\n          font-size: 1.1em;\\n        }\\n      `}</style>\",\"StartLine\":80,\"TargetContent\":\"        @media (min-width: 769px) {\\n          .mobile-art { display: none; }\\n        }\\n      `}</style>\"}]"

========================

Step 356 - Reader.jsx:
"import { useState, useEffect } from 'react';\nimport { X, Moon, Sun, ChevronLeft, ChevronRight, Menu } from 'lucide-react';\n\nexport default function Reader({ chapters, onClose }) {\n  const [theme, setTheme] = useState('dark');\n  const [activeChapterIdx, setActiveChapterIdx] = useState(0);\n  const [activePageIdx, setActivePageIdx] = useState(0);\n  const [menuOpen, setMenuOpen] = useState(false);\n  \n  const [zoom, setZoom] = useState(100);\n  const [pageInput, setPageInput] = useState('1');\n  const [expandedChapters, setExpandedChapters] = useState([0]);\n\n  // Quando o capítulo mudar, volta para a primeira página\n  useEffect(() => {\n    setActivePageIdx(0);\n    if (!expandedChapters.includes(activeChapterIdx)) {\n      setExpandedChapters([...expandedChapters, activeChapterIdx]);\n    }\n  }, [activeChapterIdx]);\n\n  useEffect(() => {\n    setPageInput((activePageIdx + 1).toString());\n  }, [activePageIdx]);\n\n  const chapter = chapters && chapters.length > 0 ? chapters[activeChapterIdx] : null;\n  const page = chapter && chapter.pages && chapter.pages.length > 0 ? chapter.pages[activePageIdx] : null;\n\n  const handleNextPage = () => {\n    if (chapter && activePageIdx < chapter.pages.length - 1) {\n      setActivePageIdx(activePageIdx + 1);\n    } else if (activeChapterIdx < chapters.length - 1) {\n      setActiveChapterIdx(activeChapterIdx + 1);\n    }\n  };\n\n  const handlePrevPage = () => {\n    if (activePageIdx > 0) {\n      setActivePageIdx(activePageIdx - 1);\n    } else if (activeChapterIdx > 0) {\n      setActiveChapterIdx(activeChapterIdx - 1);\n      const prevChapter = chapters[activeChapterIdx - 1];\n      setActivePageIdx(prevChapter.pages ? prevChapter.pages.length - 1 : 0);\n    }\n  };\n\n  const handlePageSubmit = (e) => {\n    e.preventDefault();\n    const val = parseInt(pageInput);\n    if (!isNaN(val) && chapter && val >= 1 && val <= chapter.pages.length) {\n      setActivePageIdx(val - 1);\n    } else {\n      setPageInput((activePageIdx + 1).toString());\n    }\n  };\n\
<truncated 13282 bytes>

========================

Step 376 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":20,\"ReplacementContent\":\"  const [expandedChapter, setExpandedChapter] = useState(0);\\n\\n  // Quando o capítulo mudar, volta para a primeira página\\n  useEffect(() => {\\n    setActivePageIdx(0);\\n    setExpandedChapter(activeChapterIdx);\\n  }, [activeChapterIdx]);\",\"StartLine\":12,\"TargetContent\":\"  const [expandedChapters, setExpandedChapters] = useState([0]);\\n\\n  // Quando o capítulo mudar, volta para a primeira página\\n  useEffect(() => {\\n    setActivePageIdx(0);\\n    if (!expandedChapters.includes(activeChapterIdx)) {\\n      setExpandedChapters([...expandedChapters, activeChapterIdx]);\\n    }\\n  }, [activeChapterIdx]);\"},{\"AllowMultiple\":false,\"EndLine\":66,\"ReplacementContent\":\"  const toggleChapter = (idx) => {\\n    setExpandedChapter(expandedChapter === idx ? null : idx);\\n  };\\n\\n  const getGroupedSubthemes = (pages) => {\\n    if (!pages) return [];\\n    const groups = [];\\n    pages.forEach((p, pIdx) => {\\n      if (p.subtheme) {\\n        if (groups.length === 0 || groups[groups.length - 1].name !== p.subtheme) {\\n          groups.push({ name: p.subtheme, startIdx: pIdx });\\n        }\\n      }\\n    });\\n    return groups;\\n  };\",\"StartLine\":60,\"TargetContent\":\"  const toggleChapter = (idx) => {\\n    if (expandedChapters.includes(idx)) {\\n      setExpandedChapters(expandedChapters.filter(i => i !== idx));\\n    } else {\\n      setExpandedChapters([...expandedChapters, idx]);\\n    }\\n  };\"},{\"AllowMultiple\":false,\"EndLine\":242,\"ReplacementContent\":\"          {chapters.map((ch, idx) => {\\n            const groupedSubthemes = getGroupedSubthemes(ch.pages);\\n            return (\\n              <div key={ch.id} className=\\\"chapter-wrapper\\\">\\n                <div \\n                  className={`chapter-item ${idx === activeChapterIdx ? 'active' : ''}`}\\n                  onClick={() => { setActiveChapterIdx(idx); toggleChapter(idx); setMenuOpen(false); }}\\n                >\\n                  <span>Cap. {String(idx + 1).padStart(2, '0')} - {ch.title || 'Sem título'}</span>\n<truncated 2597 bytes>"

========================

Step 382 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":146,\"ReplacementContent\":\"        @media (max-width: 1024px) {\\n          .reader-right { display: none; }\\n          .reader-header { flex-direction: column !important; gap: 1rem; padding: 1rem !important; }\\n          .header-left, .header-center, .header-right { width: 100% !important; justify-content: center !important; text-align: center; border: none !important; }\\n          .header-center { flex-direction: column !important; gap: 1rem !important; }\\n          .header-left { justify-content: flex-start !important; }\\n        }\",\"StartLine\":141,\"TargetContent\":\"        @media (max-width: 1024px) {\\n          .reader-right { display: none; }\\n          .reader-header { flex-direction: column; gap: 1rem; padding: 1rem; }\\n          .header-left, .header-center, .header-right { width: 100% !important; justify-content: center !important; text-align: center; }\\n          .header-left { justify-content: flex-start !important; }\\n        }\"},{\"AllowMultiple\":false,\"EndLine\":206,\"ReplacementContent\":\"      <div className=\\\"reader-header\\\" style={{ display: 'flex', padding: 0, borderBottom: `1px solid ${borderColor}`, background: panelBg, minHeight: '70px' }}>\\n        \\n        {/* Left Header - Matches Sidebar */}\\n        <div className=\\\"header-left\\\" style={{ width: '280px', padding: '0 1rem', display: 'flex', alignItems: 'center', borderRight: `1px solid ${borderColor}`, flexShrink: 0 }}>\\n          <button className=\\\"hamburger-btn\\\" onClick={() => setMenuOpen(!menuOpen)}>\\n            <Menu size={24} />\\n          </button>\\n          <h2 style={{ fontFamily: \\\"'Playfair Display', serif\\\", margin: 0, fontSize: '1.2rem', color: 'var(--accent-gold)' }}>Jardim das Flores</h2>\\n        </div>\\n\\n        {/* Center Header - Matches Center Column */}\\n        <div className=\\\"header-center\\\" style={{ flex: 1, padding: '0 4rem', display: 'flex', alignItems: 'center', justifyItems: 'center', justifyContent: 'space-between' }}>\\n          \\n          <div style={{ display: 'flex', fle\n<truncated 6486 bytes>"

========================

Step 400 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":35,\"ReplacementContent\":\"  const [activeChapterIdx, setActiveChapterIdx] = useState(0);\\n  const [activeSubthemeIdx, setActiveSubthemeIdx] = useState(0);\\n  const [activeColumnIdx, setActiveColumnIdx] = useState(0);\\n  const [totalColumns, setTotalColumns] = useState(1);\\n  const [menuOpen, setMenuOpen] = useState(false);\\n  const textContainerRef = useRef(null);\\n  \\n  const [zoom, setZoom] = useState(100);\\n  const [pageInput, setPageInput] = useState('1');\\n  const [expandedChapter, setExpandedChapter] = useState(0);\\n\\n  // Quando o capítulo mudar\\n  useEffect(() => {\\n    setActiveSubthemeIdx(0);\\n    setActiveColumnIdx(0);\\n    setExpandedChapter(activeChapterIdx);\\n  }, [activeChapterIdx]);\\n\\n  // Auto-Pagination Calculation\\n  useEffect(() => {\\n    const calculateColumns = () => {\\n      if (textContainerRef.current) {\\n        const container = textContainerRef.current;\\n        const columns = Math.ceil(container.scrollWidth / container.clientWidth);\\n        setTotalColumns(columns || 1);\\n        \\n        // Se a coluna ativa for 9999 (veio da navegação 'Anterior'), vai para a última\\n        if (activeColumnIdx >= columns) {\\n          setActiveColumnIdx(columns - 1);\\n        }\\n      }\\n    };\\n    \\n    setTimeout(calculateColumns, 150); // Delay for CSS/images\\n    window.addEventListener('resize', calculateColumns);\\n    return () => window.removeEventListener('resize', calculateColumns);\\n  }, [chapters, activeChapterIdx, activeSubthemeIdx, zoom]);\\n\\n  useEffect(() => {\\n    setPageInput((activeColumnIdx + 1).toString());\\n  }, [activeColumnIdx]);\\n\\n  const chapter = chapters && chapters.length > 0 ? chapters[activeChapterIdx] : null;\\n  const subthemeObj = chapter && chapter.pages && chapter.pages.length > 0 ? chapter.pages[activeSubthemeIdx] : null;\\n\\n  const handleNextPage = () => {\\n    if (activeColumnIdx < totalColumns - 1) {\\n      setActiveColumnIdx(activeColumnIdx + 1);\\n    } else if (chapter && activeSubthemeIdx < chapter.pages.length - 1) \n<truncated 12618 bytes>"

========================

Step 441 - Reader.jsx:
"import { useState, useEffect, useRef } from 'react';"

========================

Step 454 - Reader.jsx:
"                    {groupedSubthemes.map((sub, sIdx) => {\n                      const nextSub = groupedSubthemes[sIdx + 1];\n                      const isActiveSub = idx === activeChapterIdx && \n                                          activeSubthemeIdx >= sub.startIdx && \n                                          (!nextSub || activeSubthemeIdx < nextSub.startIdx);\n\n                      return (\n                        <div \n                          key={sIdx} \n                          className={`subtheme-item ${isActiveSub ? 'active' : ''}`} \n                          onClick={() => { setActiveChapterIdx(idx); setActiveSubthemeIdx(sub.startIdx); setActiveColumnIdx(0); setMenuOpen(false); }}\n                        >"

========================

Step 475 - Reader.jsx:
"  const [menuOpen, setMenuOpen] = useState(false);\n  const textContainerRef = useRef(null);\n  const measurerRefs = useRef({});\n  \n  const [zoom, setZoom] = useState(100);\n  const [pageInput, setPageInput] = useState('1');\n  const [expandedChapter, setExpandedChapter] = useState(0);\n\n  const [globalPageCounts, setGlobalPageCounts] = useState([]);\n\n  // Criar array plano de todos os subtemas do livro para paginação global\n  const allSubthemes = chapters.reduce((acc, ch, cIdx) => {\n    ch.pages?.forEach((p, pIdx) => {\n      acc.push({ chIdx: cIdx, subIdx: pIdx, text: p.text });\n    });\n    return acc;\n  }, []);\n\n  // Quando o capítulo mudar, zera a subtheme e ajusta accordion\n  useEffect(() => {\n    setExpandedChapter(activeChapterIdx);\n  }, [activeChapterIdx]);\n\n  // Medição global de todas as páginas (CSS Columns)\n  useEffect(() => {\n    const calculateAll = () => {\n      const counts = [];\n      allSubthemes.forEach((sub, i) => {\n        const el = measurerRefs.current[i];\n        if (el) {\n          const cols = Math.ceil(el.scrollWidth / el.clientWidth);\n          counts[i] = cols || 1;\n        }\n      });\n      setGlobalPageCounts(counts);\n\n      // Também medir a aba visível por precaução (deve bater com a oculta)\n      if (textContainerRef.current) {\n        const container = textContainerRef.current;\n        const cols = Math.ceil(container.scrollWidth / container.clientWidth);\n        setTotalColumns(cols || 1);\n        if (activeColumnIdx >= cols) setActiveColumnIdx(cols - 1);\n      }\n    };\n    \n    setTimeout(calculateAll, 150);\n    window.addEventListener('resize', calculateAll);\n    return () => window.removeEventListener('resize', calculateAll);\n  }, [chapters, activeChapterIdx, activeSubthemeIdx, zoom]); // refaz se zoom mudar\n\n  const chapter = chapters && chapters.length > 0 ? chapters[activeChapterIdx] : null;\n  const subthemeObj = chapter && chapter.pages && chapter.pages.length > 0 ? chapter.pages[activeSubthemeIdx] : null;\n\n  // E
<truncated 3064 bytes>

========================

Step 486 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":283,\"ReplacementContent\":\"              <form onSubmit={handlePageSubmit} style={{ display: 'flex', alignItems: 'center' }}>\\n                <input \\n                  type=\\\"text\\\" \\n                  value={pageInput} \\n                  onChange={(e) => setPageInput(e.target.value)} \\n                  onBlur={handlePageSubmit}\\n                  style={{ width: '45px', background: 'transparent', border: `1px solid ${borderColor}`, color: textColor, textAlign: 'center', borderRadius: '4px', padding: '0.2rem' }}\\n                />\\n              </form>\\n              <span style={{ fontSize: '0.9rem', opacity: 0.8 }}>/ {totalBookPages}</span>\",\"StartLine\":273,\"TargetContent\":\"              <form onSubmit={handlePageSubmit} style={{ display: 'flex', alignItems: 'center' }}>\\n                <input \\n                  type=\\\"text\\\" \\n                  value={pageInput} \\n                  onChange={(e) => setPageInput(e.target.value)} \\n                  onBlur={handlePageSubmit}\\n                  style={{ width: '35px', background: 'transparent', border: `1px solid ${borderColor}`, color: textColor, textAlign: 'center', borderRadius: '4px', padding: '0.2rem' }}\\n                />\\n              </form>\\n              <span style={{ fontSize: '0.9rem', opacity: 0.8 }}>/ {totalColumns}</span>\"},{\"AllowMultiple\":false,\"EndLine\":357,\"ReplacementContent\":\"        <div className=\\\"reader-center\\\" onClick={() => setMenuOpen(false)}>\\n          <div className=\\\"reader-content\\\">\\n            <div \\n              className=\\\"reader-text-inner\\\" \\n              ref={textContainerRef}\\n              style={{ transform: `translateX(calc(${activeColumnIdx} * -100%))` }}\\n            >\\n              <div dangerouslySetInnerHTML={{ __html: subthemeObj?.text || \\\"<p>Conteúdo não encontrado.</p>\\\" }} />\\n            </div>\\n\\n            {/* Medidores ocultos para computar páginas globais */}\\n            {allSubthemes.map((sub, i) => (\\n              <div \\n                key={i} \\n  \n<truncated 2543 bytes>"

========================

Step 498 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":232,\"ReplacementContent\":\"        .chapter-subthemes { padding: 0.5rem 0; background: rgba(0,0,0,0.05); }\\n        .subtheme-item { padding: 0.5rem 1.5rem 0.5rem 3rem; cursor: pointer; font-size: 0.9rem; opacity: 0.8; transition: all 0.2s; border-left: 3px solid transparent; }\\n        .subtheme-item:hover { opacity: 1; background: rgba(128,128,128,0.1); }\\n        .subtheme-item.active { opacity: 1; border-left-color: var(--accent-gold); color: var(--accent-gold); font-weight: 600; background: rgba(128,128,128,0.1); }\\n        \\n        .hamburger-btn { display: none; background: transparent; border: none; color: ${textColor}; cursor: pointer; margin-right: 1rem; }\\n\\n        .drop-cap-wrapper > p:first-of-type::first-letter {\\n          float: left;\\n          font-size: 3.8em;\\n          line-height: 0.85;\\n          padding-right: 0.15rem;\\n          padding-top: 0.1rem;\\n          font-family: 'Playfair Display', serif;\\n          color: var(--accent-gold);\\n          font-weight: bold;\\n        }\",\"StartLine\":226,\"TargetContent\":\"        .chapter-subthemes { padding: 0.5rem 0; background: rgba(0,0,0,0.05); }\\n        .subtheme-item { padding: 0.5rem 1.5rem 0.5rem 3rem; cursor: pointer; font-size: 0.9rem; opacity: 0.8; transition: all 0.2s; border-left: 3px solid transparent; }\\n        .subtheme-item:hover { opacity: 1; background: rgba(128,128,128,0.1); }\\n        .subtheme-item.active { opacity: 1; border-left-color: var(--accent-gold); color: var(--accent-gold); font-weight: 600; background: rgba(128,128,128,0.1); }\\n        \\n        .hamburger-btn { display: none; background: transparent; border: none; color: ${textColor}; cursor: pointer; margin-right: 1rem; }\"},{\"AllowMultiple\":false,\"EndLine\":371,\"ReplacementContent\":\"  const renderSubthemeContent = (text, chTitle, subTitle, isFirst) => {\\n    return (\\n      <div style={{ paddingBottom: '2rem' }}>\\n        {isFirst ? (\\n          <div style={{ textAlign: 'center', marginBottom: '3rem', marginTop: '1rem' }}>\\n      \n<truncated 3542 bytes>"

========================

Step 1274 - Reader.jsx:
"import { useState } from 'react';\nimport { X, Sun, Moon, ChevronLeft, ChevronRight, Minus, Plus } from 'lucide-react';\n\nexport default function Reader({ book, chapters, onClose }) {\n  const [activeChapterIdx, setActiveChapterIdx] = useState(0);\n  const [activeSubthemeIdx, setActiveSubthemeIdx] = useState(0);\n  const [zoom, setZoom] = useState(100);\n  const [theme, setTheme] = useState('dark');\n\n  // Proteção contra dados vazios\n  const safeChapters = chapters || [];\n  const currentChapter = safeChapters[activeChapterIdx] || null;\n  const subthemes = currentChapter?.pages || []; // Compatibilidade com estrutura antiga (pages)\n  const currentSubtheme = subthemes[activeSubthemeIdx] || null;\n\n  // Temas\n  const isDark = theme === 'dark';\n  const colors = {\n    bg: isDark ? '#1a1a1e' : '#f5f5f5',\n    sidebarBg: isDark ? '#1f1f23' : '#e0e0e0',\n    headerBg: isDark ? '#141418' : '#e0e0e0',\n    text: isDark ? '#e2e8f0' : '#1a1a1a',\n    accent: '#d4af37',\n    border: isDark ? '#333' : '#ccc',\n  };\n\n  const handleNextPage = () => {\n    if (activeSubthemeIdx < subthemes.length - 1) {\n      setActiveSubthemeIdx(activeSubthemeIdx + 1);\n    } else if (activeChapterIdx < safeChapters.length - 1) {\n      setActiveChapterIdx(activeChapterIdx + 1);\n      setActiveSubthemeIdx(0);\n    }\n  };\n\n  const handlePrevPage = () => {\n    if (activeSubthemeIdx > 0) {\n      setActiveSubthemeIdx(activeSubthemeIdx - 1);\n    } else if (activeChapterIdx > 0) {\n      setActiveChapterIdx(activeChapterIdx - 1);\n      const prevCh = safeChapters[activeChapterIdx - 1];\n      setActiveSubthemeIdx(prevCh.pages ? prevCh.pages.length - 1 : 0);\n    }\n  };\n\n  return (\n    <div style={{\n      position: 'fixed', top: 0, left: 0, right: 0, bottom: 0,\n      backgroundColor: colors.bg, color: colors.text, zIndex: 9999,\n      display: 'flex', flexDirection: 'column', fontFamily: 'Arial, sans-serif'\n    }}>\n      {/* ── TOP HEADER ── */}\n      <header style={{\n        display: 'flex', alignItems: 'cent
<truncated 8433 bytes>

========================

Step 1280 - Reader.jsx:
"import { useState, useRef, useEffect } from 'react';\nimport { X, Type, Eye, Menu, ChevronLeft, ChevronRight } from 'lucide-react';\n\nexport default function Reader({ book, chapters, onClose }) {\n  const [activeChapterIdx, setActiveChapterIdx] = useState(0);\n  const [activeSubthemeIdx, setActiveSubthemeIdx] = useState(0);\n  \n  const safeChapters = chapters || [];\n  const currentChapter = safeChapters[activeChapterIdx] || null;\n  const subthemes = currentChapter?.pages || [];\n  const currentSubtheme = subthemes[activeSubthemeIdx] || null;\n\n  const [fontSize, setFontSize] = useState(1.2); // rem\n  const [progress, setProgress] = useState(0);\n  const contentRef = useRef(null);\n\n  const totalPages = subthemes.length > 0 ? subthemes.length : 1;\n  const currentPage = activeSubthemeIdx + 1;\n\n  useEffect(() => {\n    setProgress((currentPage / totalPages) * 100);\n  }, [currentPage, totalPages]);\n\n  const handleNext = () => {\n    if (activeSubthemeIdx < subthemes.length - 1) {\n      setActiveSubthemeIdx(activeSubthemeIdx + 1);\n    } else if (activeChapterIdx < safeChapters.length - 1) {\n      setActiveChapterIdx(activeChapterIdx + 1);\n      setActiveSubthemeIdx(0);\n    }\n  };\n\n  const handlePrev = () => {\n    if (activeSubthemeIdx > 0) {\n      setActiveSubthemeIdx(activeSubthemeIdx - 1);\n    } else if (activeChapterIdx > 0) {\n      setActiveChapterIdx(activeChapterIdx - 1);\n      const prevCh = safeChapters[activeChapterIdx - 1];\n      setActiveSubthemeIdx(prevCh.pages ? prevCh.pages.length - 1 : 0);\n    }\n  };\n\n  return (\n    <div style={{\n      position: 'fixed', top: 0, left: 0, right: 0, bottom: 0,\n      backgroundColor: '#16161e', color: '#e0e0e0', zIndex: 9999,\n      display: 'flex', flexDirection: 'column', fontFamily: \"'Inter', sans-serif\"\n    }}>\n      <style>{`\n        .reader-body {\n          font-family: 'Merriweather', 'Georgia', serif;\n          line-height: 1.9;\n          color: #d1d5db;\n        }\n        .reader-body p {\n          margin-bottom: 1.5rem
<truncated 6217 bytes>

========================

Step 1289 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":137,\"ReplacementContent\":\"            style={{ fontSize: `${fontSize}rem` }}\",\"StartLine\":137,\"TargetContent\":\"            style={{ fontSize: \\\\`\\\\${fontSize}rem\\\\` }}\"},{\"AllowMultiple\":false,\"EndLine\":165,\"ReplacementContent\":\"          <div style={{ width: `${progress}%`, height: '100%', backgroundColor: '#d4af37', transition: 'width 0.3s ease' }} />\",\"StartLine\":163,\"TargetContent\":\"          <div style={{ width: \\\\`\\\\${progress}%\\\\`, height: '100%', backgroundColor: '#d4af37', transition: 'width 0.3s ease' }} />\"}]"

========================

Step 1295 - Reader.jsx:
"[{\"AllowMultiple\":false,\"EndLine\":45,\"ReplacementContent\":\"    <div style={{\\n      position: 'fixed', top: 0, left: 0, right: 0, bottom: 0,\\n      backgroundColor: '#f9f6f0', color: '#2b2b2b', zIndex: 9999,\\n      display: 'flex', flexDirection: 'column', fontFamily: \\\"'Inter', sans-serif\\\"\\n    }}>\\n      <style>{`\\n        .reader-body {\\n          font-family: 'Merriweather', 'Georgia', serif;\\n          line-height: 1.9;\\n          color: #2b2b2b;\\n        }\\n        .reader-body p {\\n          margin-bottom: 1.5rem;\\n          text-align: justify;\\n        }\\n        .reader-body h1, .reader-body h2, .reader-body h3 {\\n          font-family: 'Playfair Display', serif;\\n          color: #8b0000;\\n          margin-top: 2rem;\\n          margin-bottom: 1rem;\\n          text-align: left;\\n        }\\n        /* Drop Cap Effect ONLY on the first page of a chapter */\\n        .first-page-content > p:first-of-type::first-letter {\\n          font-size: 4.5rem;\\n          float: left;\\n          line-height: 0.8;\\n          margin-right: 0.8rem;\\n          margin-top: 0.2rem;\\n          color: #8b0000;\\n          font-family: 'Playfair Display', serif;\\n          font-weight: bold;\\n        }\\n        \\n        /* Custom Scrollbar */\\n        ::-webkit-scrollbar { width: 8px; }\\n        ::-webkit-scrollbar-track { background: #f9f6f0; }\\n        ::-webkit-scrollbar-thumb { background: #d1cbbd; border-radius: 4px; }\\n        ::-webkit-scrollbar-thumb:hover { background: #8b0000; }\\n      `}</style>\",\"StartLine\":40,\"TargetContent\":\"    <div style={{\\n      position: 'fixed', top: 0, left: 0, right: 0, bottom: 0,\\n      backgroundColor: '#16161e', color: '#e0e0e0', zIndex: 9999,\\n      display: 'flex', flexDirection: 'column', fontFamily: \\\"'Inter', sans-serif\\\"\\n    }}>\\n      <style>{`\\n        .reader-body {\\n          font-family: 'Merriweather', 'Georgia', serif;\\n          line-height: 1.9;\\n          color: #d1d5db;\\n        }\\n        .reader-body p {\\n          margin-bottom: 1.5rem;\\n          text-align: justi\n<truncated 10807 bytes>"

========================

Step 282 - Reader.jsx:
"import { useState } from 'react';\nimport { Menu, X, ChevronLeft, ChevronRight, Moon, Sun, ArrowLeft } from 'lucide-react';\n\nexport default function Reader({ data, onClose }) {\n  const [theme, setTheme] = useState('dark'); // 'dark' | 'light'\n  const [currentChapterIdx, setCurrentChapterIdx] = useState(0);\n  const [currentPageIdx, setCurrentPageIdx] = useState(0);\n  const [isSidebarOpen, setIsSidebarOpen] = useState(false);\n\n  const chapters = data.chapters || [];\n  const currentChapter = chapters[currentChapterIdx];\n  const currentPage = currentChapter?.pages?.[currentPageIdx];\n\n  const handleNextPage = () => {\n    if (!currentChapter) return;\n    if (currentPageIdx < currentChapter.pages.length - 1) {\n      setCurrentPageIdx(currentPageIdx + 1);\n    } else if (currentChapterIdx < chapters.length - 1) {\n      setCurrentChapterIdx(currentChapterIdx + 1);\n      setCurrentPageIdx(0);\n    }\n  };\n\n  const handlePrevPage = () => {\n    if (!currentChapter) return;\n    if (currentPageIdx > 0) {\n      setCurrentPageIdx(currentPageIdx - 1);\n    } else if (currentChapterIdx > 0) {\n      setCurrentChapterIdx(currentChapterIdx - 1);\n      setCurrentPageIdx(chapters[currentChapterIdx - 1].pages.length - 1);\n    }\n  };\n\n  const handleChapterSelect = (idx) => {\n    setCurrentChapterIdx(idx);\n    setCurrentPageIdx(0);\n    setIsSidebarOpen(false);\n  };\n\n  const colors = {\n    dark: { bg: '#121212', text: '#e0e0e0', panelBg: '#1e1e1e', border: '#333' },\n    light: { bg: '#fdfcf0', text: '#2d2d2d', panelBg: '#f4f2e6', border: '#e0ddd0' }\n  };\n\n  const themeColors = colors[theme];\n\n  return (\n    <div style={{\n      position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, zIndex: 1000,\n      backgroundColor: themeColors.bg, color: themeColors.text,\n      fontFamily: \"'Inter', sans-serif\",\n      display: 'flex', flexDirection: 'column'\n    }}>\n      {/* Navbar */}\n      <div style={{\n        height: '60px', display: 'flex', alignItems: 'center', justifyContent: 'space-between',
<truncated 7168 bytes>