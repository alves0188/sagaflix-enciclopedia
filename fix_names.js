const fs = require('fs'); const path = require('path'); const dir = 'src/components'; const replaceDisplay = (str, objName) => str.replace(new RegExp(objName + '\\.name', 'g'), (.displayMode === 'name' ? .name : (.nickname || .name))); const files = fs.readdirSync(dir).filter(f => f.endsWith('.jsx')); for(let file of files) { let p = path.join(dir, file); let content = fs.readFileSync(p, 'utf8'); content = replaceDisplay(content, 'currentUser'); content = replaceDisplay(content, 'author'); content = replaceDisplay(content, 'authorObj'); content = replaceDisplay(content, 'selectedAuthor'); content = replaceDisplay(content, 'bookAuthor'); content = replaceDisplay(content, 'newUser'); fs.writeFileSync(p, content); } console.log('Done');
